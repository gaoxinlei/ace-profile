[merge]
    tool = meld

[diff]
    tool = meld

[user]
    name  = acefei
    email = acefeilxy@gmail.com

[credential]
    helper = store

[push]
    default = simple

[alias]
    up = !sh -c 'git pull --rebase && git push'
